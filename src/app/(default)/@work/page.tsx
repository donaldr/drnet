import WorkSection from "@/app/(default)/@work/section";
import WorkItems from "@/app/(default)/@work/workitems";

export default function Work() {
  return (
    <WorkSection>
      <WorkItems />
    </WorkSection>
  );
}
